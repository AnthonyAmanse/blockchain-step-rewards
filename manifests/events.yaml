---
apiVersion: v1
kind: Service
metadata:
  name: events
  labels:
    app: events
spec:
  ports:
    - port: 8080
      protocol: TCP
      targetPort: 8080
  type: ClusterIP
  selector:
    app: events
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: events-deployment
  labels:
    app: events
spec:
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: events
    spec:
      containers:
        - image: anthonyamanse/events:test-event
          imagePullPolicy: Always
          name: events
          env:
            - name: MONGODB_URL
              valueFrom:
                configMapKeyRef:
                  name: mongodb-url
                  key: MONGODB_URL
          volumeMounts:
            - name: mongodb-cert
              mountPath: "/etc/ssl"
              readOnly: true
          ports:
            - containerPort: 8080
      volumes:
        - name: mongodb-cert
          secret:
            secretName: mongodb-cert-secret
