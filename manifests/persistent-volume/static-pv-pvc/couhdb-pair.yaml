apiVersion: v1
kind: PersistentVolume
metadata:
  name: couchdb-volume
spec:
  capacity:
    storage: "20Gi"
  accessModes:
    - ReadWriteMany
  claimRef:
    namespace: default
    name: couchdb-claim
  nfs:
    server: "fsf-dal1301a-fz.adn.networklayer.com"
    path: "/IBM02SEV1323471_1067/data01"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  annotations:
    volume.beta.kubernetes.io/storage-class: "" # Comment this line to use dynamic provisioning (paid)
  name: couchdb-claim
  labels:
    app: couchdb-volume
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
